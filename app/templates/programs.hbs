{{outlet}}

<h2>{{name}}</h2>
 <ul>
   <h4>{{link-to "Add New Program" 'programs.new'}}</h4>
   {{#each model as |program|}}
    <p>{{link-to program.title 'programs.program' program}}</p>
   {{/each}}
 </ul>
